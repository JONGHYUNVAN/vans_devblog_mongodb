# MongoDB 환경 변수 예시
# 실제 사용 시 .env 파일로 복사하여 사용하세요

# MongoDB 관리자 계정
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=changeme_strong_password_here

# 초기 데이터베이스 이름
MONGO_INITDB_DATABASE=devblog

# MongoDB 연결 URI (애플리케이션에서 사용)
# 내부 연결 (Cloudtype 프로젝트 내)
MONGODB_URI=mongodb://admin:changeme_strong_password_here@devblog-mongodb:27017/devblog?authSource=admin

# 외부 연결 (로컬 개발 시)
# MONGODB_URI=mongodb://admin:changeme_strong_password_here@localhost:27017/devblog?authSource=admin

